# .NET build output
bin/
obj/
*_wpftmp.csproj

# Packaging artifacts (installer EXE / portable zip) — pushed separately by hand
PAKE/

# Transient build dir / logs
build/
build_out.txt
selftest_result.txt
obj2/
tmp_build/

# Agent workspace metadata (local only)
.reasonix/
reasonix.toml

# 内置免费 AI 密钥（构建时由 scripts/embed_free_ai_key.ps1 生成；真实密钥绝不入库）
OBS_Helper.Wpf/Assets/free_ai_key.json
local/
